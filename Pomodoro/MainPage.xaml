<UserControl x:Class="Pomodoro.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400" DataContext="{Binding}">

    <Grid x:Name="LayoutRoot" Background="White">
        <Button x:Name="Start" Height="36" Width="80" Content="Start" Command="{Binding Start}" Margin="12,12,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" />
        <TextBlock x:Name="timeRemaining" Height="36" Margin="98,12,12,0" Text="{Binding TimeRemaining}" VerticalAlignment="Top" FontSize="32" FontFamily="Trebuchet MS" />
    </Grid>
</UserControl>
